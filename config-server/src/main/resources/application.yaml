server:
  port: 8761

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ds3128/admissionschool-config.git
          username: dariussteves@yahoo.fr
          password: Angelle1997@
          default-label: main
          clone-on-start: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh