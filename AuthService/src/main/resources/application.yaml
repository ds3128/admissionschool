server:
  port: 8081

spring:
  application:
    name: AuthService
  datasource:
    driver-class-name: org.postgresql.Driver
    username: auth_service
    password: auth_service#123@
    url: jdbc:postgresql://localhost:5432/auth_db?createDatabaseIfNoExist=true
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: dariussteves44@gmail.com
    password: owva xtvy iedo iywm
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  base-url: http://localhost:8081